name: Workflow 1
on: 
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Print Greeting
        run: echo "Hello, World!"
      - name: Print Ending Message
        run: echo "Done, GoodBye!"
